package guiTest;

public class guiTest {

	public static void main(String[] args) {

		//Jframe = a GUI window to add components to
		
		/*JFrame frame= new JFrame(); //create a frame
		
		frame.setTitle("JFrame title goes here");	//set title of frame
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);	//exit out of application
		frame.setResizable(false);	//prevent frame from being resized, also prevent full screen
		frame.setSize(300, 300);	//set x dimension, set y dimension
		frame.setVisible(true);		//make the frame visible
		
		ImageIcon imgicon= new ImageIcon("logo.png"); //create an image icon
		frame.setIconImage(imgicon.getImage()); //change icon of frame
		
		frame.getContentPane().setBackground(Color.green); 			//change color of background
		//set color variations
		//frame.getContentPane().setBackground(new Color(255, 0, 0));	//change color of background
		//frame.getContentPane().setBackground(new Color(0x123456));		//hex color
		*/
		
		MyFrame myframe= new MyFrame();
	}

}
